# Midjourney 파라미터 사용하기

Midjourney 고급 파라미터를 활용하여 더 정교한 이미지를 생성하는 방법을 알아봅니다.

## 개요

Midjourney는 --sref, --sw, --ar 등 다양한 파라미터를 지원합니다. PromptBlocks에서는 Assemble에서 이러한 파라미터를 쉽게 추가할 수 있습니다.

## 단계별 가이드

### 1. Midjourney 모드 선택

1. **Assemble** 페이지로 이동
2. **Output Mode** 섹션 찾기
3. **"Midjourney"** 선택

### 2. SREF (Style Reference) 설정

**SREF 코드 입력**:

1. Midjourney에서 생성한 스타일 참조 코드 입력
2. 예시: `123456 987654::2`
3. 여러 SREF 코드 공백으로 구분

> 💡 **팁**: SREF는 참조 이미지의 스타일을 새 이미지에 적용합니다.

### 3. Style Weight 조정

**--sw (Style Weight)**:

- 범위: 0-1000
- 기본값: 100
- 높을수록 SREF 스타일 강하게 적용

**조정 방법**:

1. 슬라이더로 조정 (0-1000)
2. 또는 직접 숫자 입력

### 4. Style Version 선택

**--sv (Style Version)**:

- **v7 default (6)**: 최신 버전
- **legacy/default (4)**: 이전 버전
- **사용 안 함**: 버전 파라미터 제외

### 5. Negative Prompt

**제외할 요소 지정**:

```
--no text, watermark, blurry, low quality
```

PromptBlocks에서:

1. **Negative Prompt** 필드에 입력
2. 쉼표로 구분

### 6. Aspect Ratio

기본 프롬프트에 직접 추가:

```
--ar 16:9  (와이드)
--ar 9:16  (세로)
--ar 1:1   (정사각형)
--ar 2:3   (인물용)
```

### 7. 기타 유용한 파라미터

**품질 관련**:

```
--q 2       (최고 품질)
--hd        (고해상도)
--uplight   (부드러운 업스케일)
```

**창의성 관련**:

```
--s 750     (스타일라이제이션, 높을수록 예술적)
--c 50      (카오스, 높을수록 다양한 결과)
```

**시드값**:

```
--seed 12345  (재현 가능한 결과)
```

## 파라미터 조합 예시

### 예시 1: 일관된 스타일 시리즈

```
beautiful Korean woman in white dress, soft lighting,
--sref 123456 --sw 200 --ar 2:3 --seed 12345
```

### 예시 2: 빈티지 사진 스타일

```
portrait of elderly man, wrinkled face, black and white,
--sref 789012 --sw 150 --ar 4:5 --no color
```

### 예시 3: 애니메이션 스타일

```
cute anime character, cherry blossom background,
--sref 345678 --sw 300 --ar 16:9 --niji 6
```

## 팁과 모범사례

### ✅ 파라미터 우선순위

1. **주제/설명** (프롬프트 본문)
2. **스타일 참조** (--sref)
3. **비율/품질** (--ar, --q)
4. **시드/카오스** (--seed, --c)

### ✅ SREF 효과적으로 사용하기

- **스타일 전용 이미지**: 단순한 스타일 샘플 이미지
- **다중 SREF**: 여러 스타일 혼합 가능
- **적정 SW**: 너무 높으면 원본 이미지와 유사해짐

### ⚠️ 주의사항

- **버전 차이**: Midjourney 버전별 파라미터 지원 차이
- **중복 주의**: SREF와 스타일 아티스트 중복 시 충돌 가능
- **비율 제한**: 일부 기능은 특정 비율에서만 동작

## 다음 단계

- [프리셋 저장하고 공유하기](./save-share-presets.md)
- [키보드 단축키](../reference/keyboard-shortcuts.md)
